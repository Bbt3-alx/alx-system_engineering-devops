#!/usr/bin/env bash
# Display lines containing the word "bash" and show the PID
# shellcheck disable=SC2009

# Use ps to list all processes (including those without a TTY)
# Filter lines containing "bash"
# Print the PID and the line
ps aux | grep -w "bash" | awk '{print $2, $0}'
